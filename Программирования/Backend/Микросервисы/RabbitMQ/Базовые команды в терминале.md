



## 🧩 Управление через терминал (`rabbitmqctl`)

### Проверить статус сервера

```bash
rabbitmqctl status
```
### Список всех очередей

```
```bash
rabbitmqctl list_queues
```

### Список всех обменников

```bash
rabbitmqctl list_exchanges
```

### Список всех подключений

```bash
rabbitmqctl list_connections
```

### Список всех пользователей

```bash
rabbitmqctl list_users
```

---

## 👤 Пользователи и права доступа

### Добавить нового пользователя

```bash
rabbitmqctl add_user myuser mypassword
```

### Удалить пользователя

```bash
rabbitmqctl delete_user myuser
```

### Задать права пользователю

```bash
rabbitmqctl set_permissions -p / myuser ".*" ".*" ".*"
```

### Проверить права пользователя

```bash
rabbitmqctl list_user_permissions myuser
```

---

## 📦 Очереди (Queues)

### Создать очередь

```bash
rabbitmqadmin declare queue name=my_queue durable=true
```

### Удалить очередь

```bash
rabbitmqadmin delete queue name=my_queue
```

### Просмотреть все очереди и количество сообщений

```bash
rabbitmqctl list_queues name messages_ready messages_unacknowledged
```

---

## 🔄 Обменники (Exchanges)

### Создать обменник

```bash
rabbitmqadmin declare exchange name=my_exchange type=direct durable=true
```

### Удалить обменник

```bash
rabbitmqadmin delete exchange name=my_exchange
```

---

## 🔗 Привязки (Bindings)

### Привязать очередь к обменнику

```bash
rabbitmqadmin declare binding source=my_exchange destination=my_queue routing_key=my_key
```

### Удалить привязку

```bash
rabbitmqadmin delete binding source=my_exchange destination=my_queue
```

---

## 📨 Сообщения

### Отправить сообщение в очередь

```bash
rabbitmqadmin publish routing_key=my_queue payload="Hello, Rabbit!"
```

### Получить одно сообщение из очереди

```bash
rabbitmqadmin get queue=my_queue
```

### Очистить очередь

```bash
rabbitmqctl purge_queue my_queue
```

---

## 🧰 Дополнительно

### Остановить RabbitMQ

```bash
rabbitmqctl stop
```

### Перезапустить RabbitMQ

```bash
sudo systemctl restart rabbitmq-server
```

### Проверить состояние сервиса

```bash
sudo systemctl status rabbitmq-server
```

---

## 🌐 Веб-интерфейс управления

После установки плагина управления:

```bash
rabbitmq-plugins enable rabbitmq_management
```

Интерфейс будет доступен по адресу:  
👉 `http://localhost:15672`  
(по умолчанию логин/пароль: `guest / guest`)
