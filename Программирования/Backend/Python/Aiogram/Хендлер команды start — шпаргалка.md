Команда `/start` — это **первый контакт пользователя с ботом**.  
Правильная реализация хендлера важна для:

- Приветствия пользователя  
- Инициализации состояния FSM (если используется)  
- Подключения к базе данных  
- Выдачи стартовой клавиатуры  

---

## 1️⃣ Пример хендлера `/start`

```python
from aiogram import Router, F
from aiogram.types import Message
from bot.keyboards.main_keyboard import main_keyboard
from bot.services.user_service import create_or_get_user
from bot.config import settings

router = Router()

@router.message(F.text == "/start")
async def start_handler(message: Message):
    """
    Обработчик команды /start
    """
    user_id = message.from_user.id

    # Создание пользователя в БД или получение существующего
    user = await create_or_get_user(user_id, message.from_user.full_name)

    # Приветственное сообщение
    welcome_text = (
        f"Привет, {message.from_user.first_name}!\n"
        "Я бот для помощи и развлечений.\n"
        "Используй меню ниже для навигации."
    )

    # Отправка сообщения с клавиатурой
    await message.answer(welcome_text, reply_markup=main_keyboard)
````

---

## 2️⃣ Разбор компонентов

### 2.1 `Router()`

- Новый способ регистрации хендлеров в Aiogram 3.x
    
- Позволяет структурировать обработчики по файлам
    
- Подключается в `main.py` через `dp.include_router(router)`
    

### 2.2 Фильтр `F.text == "/start"`

- Сравнивает текст входящего сообщения
    
- Можно использовать и другие фильтры (`F.from_user.id`, `F.chat.type`)
    

### 2.3 `create_or_get_user`

- Функция из `services/user_service.py`
    
- Создает пользователя в базе или возвращает существующего
    
- Позволяет отслеживать статистику и состояния пользователя
    

### 2.4 `main_keyboard`

- ReplyKeyboardMarkup или InlineKeyboardMarkup
    
- Позволяет пользователю взаимодействовать с ботом сразу после старта
    

---

## 3️⃣ FSM и стартовое состояние

Если бот использует **Finite State Machine**, можно устанавливать стартовое состояние:

```python
from aiogram.fsm.context import FSMContext
from bot.states import UserState

@router.message(F.text == "/start")
async def start_handler(message: Message, state: FSMContext):
    await state.set_state(UserState.main_menu)
    await message.answer("Добро пожаловать!", reply_markup=main_keyboard)
```

- `UserState.main_menu` — состояние главного меню
    
- Позволяет контролировать логику дальнейших хендлеров
    

---

## 4️⃣ Работа с базой данных

Пример функции `create_or_get_user`:

```python
from bot.models.user import User
from database.db import async_session

async def create_or_get_user(user_id: int, full_name: str):
    async with async_session() as session:
        user = await session.get(User, user_id)
        if not user:
            user = User(id=user_id, full_name=full_name)
            session.add(user)
            await session.commit()
        return user
```

- Асинхронная работа с SQLAlchemy
    
- Создание пользователя при первом входе
    

---

## 5️⃣ Best practices для `/start`

1. Минимизируйте логику в хендлере — выносите в сервисы
    
2. Всегда проверяйте существование пользователя в БД
    
3. Используйте клавиатуры для удобства навигации
    
4. Если используется FSM, задавайте стартовое состояние
    
5. Приветственное сообщение должно быть коротким и информативным
    
6. Обработка `/start` должна быть idempotent — повторный вызов не ломает систему
    

---

## 6️⃣ Итог

- `/start` — точка входа пользователя
    
- Создает/получает пользователя в БД
    
- Отправляет приветственное сообщение с клавиатурой
    
- При использовании FSM — устанавливает стартовое состояние
    
- Должен быть лёгким, быстрым и безопасным
    

---

💡 Совет: **всегда держите хендлер `/start` максимально чистым** — только логика приветствия и инициализации. Всё остальное выносите в сервисы, клавиатуры и FSM.

