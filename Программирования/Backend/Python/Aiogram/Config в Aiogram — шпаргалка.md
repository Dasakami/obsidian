Файл `config.py` отвечает за **конфигурацию проекта**: токены бота, настройки базы данных, параметры окружения и прочие константы.  

Правильная организация конфигов помогает:
- Изолировать секретные данные от кода
- Легко менять настройки между dev/prod
- Работать с несколькими средами (development, staging, production)
- Сделать проект удобным для команды

---

## 1️⃣ Использование `.env` для конфиденциальных данных

Создаём файл `.env` в корне проекта:

```

BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11  
DATABASE_URL=sqlite+aiosqlite:///./bot.db  
ADMIN_IDS=123456789,987654321  
DEBUG=True

````

- **BOT_TOKEN** — токен бота Telegram  
- **DATABASE_URL** — URL для базы данных  
- **ADMIN_IDS** — список ID администраторов через запятую  
- **DEBUG** — режим отладки  

---

## 2️⃣ Загрузка `.env` в Python

Устанавливаем `python-dotenv`:

```bash
pip install python-dotenv
````

В `config.py`:

```python
from dotenv import load_dotenv
import os

load_dotenv()  # Загружаем все переменные из .env

BOT_TOKEN = os.getenv("BOT_TOKEN")
DATABASE_URL = os.getenv("DATABASE_URL")
ADMIN_IDS = list(map(int, os.getenv("ADMIN_IDS").split(",")))
DEBUG = os.getenv("DEBUG", "False") == "True"
```

- `os.getenv` — безопасно получает переменную окружения
    
- `split(",")` и `map(int, ...)` превращает строку в список ID
    

---

## 3️⃣ Использование Pydantic для конфигурации (рекомендовано)

Pydantic позволяет валидировать и типизировать конфигурации.

```python
from pydantic import BaseSettings
from typing import List

class Settings(BaseSettings):
    bot_token: str
    database_url: str
    admin_ids: List[int]
    debug: bool = False

    class Config:
        env_file = ".env"   # Автоматически подгружает .env

settings = Settings()
```

Используем в проекте:

```python
from bot.config import settings

BOT_TOKEN = settings.bot_token
ADMIN_IDS = settings.admin_ids
DEBUG = settings.debug
```

✅ Плюсы Pydantic:

- Типизация и валидация
    
- Автоматическое чтение `.env`
    
- Возможность задавать значения по умолчанию
    

---

## 4️⃣ Конфигурации для разных сред (dev/prod)

Стандартный подход:

```
.env                 # для локальной разработки
.env.production      # для продакшена
```

В `config.py`:

```python
import os
from pydantic import BaseSettings

class Settings(BaseSettings):
    bot_token: str
    database_url: str
    admin_ids: list[int]
    debug: bool = False

    class Config:
        env_file = os.getenv("ENV_FILE", ".env")

settings = Settings()
```

- Перед запуском указываем переменную окружения:
    

```bash
ENV_FILE=.env.production uvicorn bot.main:app --reload
```

---

## 5️⃣ Пример использования в коде

```python
from aiogram import Bot, Dispatcher
from bot.config import settings

bot = Bot(token=settings.bot_token)
dp = Dispatcher()

# Проверка админа
async def is_admin(user_id: int):
    return user_id in settings.admin_ids
```

- Все секреты и настройки централизованы
    
- Легко тестировать и менять переменные
    

---

## 6️⃣ Best practices по конфигурации

1. **Не хранить токен бота в коде** — только через `.env`
    
2. **Использовать Pydantic** для валидации типов
    
3. **Разделять среды** (dev, prod, staging)
    
4. **Использовать списки для админов и ключей**
    
5. **Использовать `DEBUG` флаг** для логирования и тестирования
    
6. **Централизовать конфиг** — все модули берут настройки из одного файла
    
7. Для больших проектов можно сделать `config/dev.py` и `config/prod.py` и подключать динамически
    

---

💡 **Итог:**

- `config.py` + `.env` + Pydantic = безопасная и гибкая конфигурация
    
- Настройки легко менять без изменения кода
    
- Конфиги централизованы и типизированы
    
- Подходит для командной работы и продакшен-проектов
    