Правильная структура бота помогает **поддерживать, масштабировать и тестировать проект**.  
Ниже приведена рекомендуемая архитектура для современных ботов на Aiogram 3.x.

---

## 1️⃣ Корневая структура

```

bot_project/  
├── bot/  
│ ├── main.py # Точка входа бота  
│ ├── config.py # Настройки и токены  
│ ├── handlers/ # Обработчики команд и сообщений  
│ ├── keyboards/ # Inline и Reply клавиатуры  
│ ├── middlewares/ # Middleware для бота  
│ ├── services/ # Логика и работа с БД  
│ ├── models/ # ORM модели  
│ ├── filters/ # Фильтры для сообщений  
│ └── utils/ # Вспомогательные функции  
├── database/ # Инициализация БД и модели  
├── requirements.txt  
├── .env # Токен и конфиденциальные настройки  
└── README.md

```

---

## 2️⃣ Подробное описание

### `bot/main.py`

- Основной файл запуска бота  
- Регистрирует хендлеры, клавиатуры, middlewares  
- Подключает логирование и планировщик задач  

---

### `bot/config.py`

- Хранит конфигурации проекта (токен, API ключи, параметры БД)  
- Лучше использовать Pydantic `BaseSettings` для валидации `.env`  

---

### `bot/handlers/`

- Все обработчики команд (`/start`, `/help`, `/admin`)  
- Разделение на файлы помогает структурировать код  
- Использует фильтры, клавиатуры и сервисы  

---

### `bot/keyboards/`

- Inline и Reply клавиатуры  
- Каждый файл для отдельного вида клавиатуры (`main_keyboard.py`, `admin_keyboard.py`)  

---

### `bot/middlewares/`

- Middleware для логирования, проверки пользователей, авторизации  
- Регистрируются в Dispatcher (`dp.update.middleware(...)`)  

---

### `bot/services/`

- Бизнес-логика бота  
- Работа с базой данных, уведомления, рассылки  
- Выносит логику из обработчиков, чтобы код был чистым  

---

### `bot/models/`

- ORM модели через SQLAlchemy или SQLModel  
- Каждая модель в отдельном файле (`user.py`, `item.py`)  
- Используются для хранения данных бота  

---

### `bot/filters/`

- Пользовательские фильтры для хендлеров  
- Пример: `is_admin.py` проверяет, является ли пользователь админом  

---

### `bot/utils/`

- Вспомогательные функции, которые часто используются  
- Пример: генерация уникальных ID, форматирование текста  

---

### `database/`

- Инициализация базы данных (`db.py`)  
- Общие модели или Base классы для ORM (`models.py`)  

---

### `.env`

- Хранение токена бота и других секретов  
- Пример:

```

BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11  
DATABASE_URL=sqlite+aiosqlite:///./bot.db

```

---

### `requirements.txt`

- Все зависимости проекта  

---

💡 **Итог:**  
- Разделение по папкам: **handlers, keyboards, services, models, middlewares, utils**  
- Конфигурации и токены хранятся отдельно  
- База данных вынесена в отдельную папку  
- Такая структура делает проект **масштабируемым и удобным для команды**
