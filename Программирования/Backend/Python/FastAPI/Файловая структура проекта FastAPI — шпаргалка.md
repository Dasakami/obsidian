Ниже приведена рекомендуемая структура проекта FastAPI для средних и больших приложений.  
Она помогает организовать код, сделать его читаемым и легко масштабируемым.

---

## 1️⃣ Корневая структура

```

project_name/  
├── app/  
│ ├── **init**.py  
│ ├── main.py  
│ ├── core/  
│ │ ├── **init**.py  
│ │ ├── config.py  
│ │ └── security.py  
│ ├── api/  
│ │ ├── **init**.py  
│ │ ├── v1/  
│ │ │ ├── **init**.py  
│ │ │ ├── endpoints/  
│ │ │ │ ├── **init**.py  
│ │ │ │ ├── users.py  
│ │ │ │ └── items.py  
│ │ │ └── dependencies.py  
│ ├── models/  
│ │ ├── **init**.py  
│ │ └── user.py  
│ ├── schemas/  
│ │ ├── **init**.py  
│ │ └── user.py  
│ ├── services/  
│ │ ├── **init**.py  
│ │ └── user_service.py  
│ ├── db/  
│ │ ├── **init**.py  
│ │ ├── session.py  
│ │ └── base.py  
│ └── utils/  
│ ├── **init**.py  
│ └── helpers.py  
├── tests/  
│ ├── **init**.py  
│ ├── test_users.py  
│ └── test_items.py  
├── requirements.txt  
├── alembic.ini  
├── migrations/  
├── README.md  
└── .env

````

---

## 2️⃣ Описание основных папок и файлов

### `app/`

- Основной пакет приложения  
- Содержит всё, связанное с логикой FastAPI  

---

### `app/main.py`

- Точка входа приложения  
- Инициализирует FastAPI, подключает роуты и middleware  

```python
from fastapi import FastAPI
from app.api.v1.endpoints import users, items

app = FastAPI(title="My Project")

app.include_router(users.router, prefix="/users", tags=["Users"])
app.include_router(items.router, prefix="/items", tags=["Items"])
````

---

### `app/core/`

- Конфигурации, настройки и безопасность
    
- `config.py` — хранит переменные окружения (через Pydantic `BaseSettings`)
    
- `security.py` — JWT, hashing паролей, OAuth2 схемы
    

---

### `app/api/`

- Папка для **REST API**
    
- Разделение на версии (`v1`, `v2`) помогает управлять разными версиями эндпоинтов
    
- `dependencies.py` — общие зависимости (Depends) для эндпоинтов
    

---

### `app/models/`

- SQLAlchemy модели
    
- Каждый файл — отдельная сущность (User, Item, Post и т.д.)
    

---

### `app/schemas/`

- Pydantic модели для **валидции входящих и исходящих данных**
    
- Разделение на `Create`, `Update`, `Out` помогает контролировать поля
    

---

### `app/services/`

- Слой бизнес-логики
    
- Например, `user_service.py` — функции работы с пользователями (CRUD, проверки)
    
- Используется для разделения логики от эндпоинтов
    

---

### `app/db/`

- Инициализация базы данных
    
- `session.py` — создание SessionLocal и engine
    
- `base.py` — Base модель для SQLAlchemy
    

---

### `app/utils/`

- Вспомогательные функции, конвертация данных, генерация токенов
    

---

### `tests/`

- Unit и integration тесты
    
- Используем `pytest` и `TestClient` для FastAPI
    
- Структура повторяет структуру `app/`
    

---

### `requirements.txt`

- Список зависимостей проекта
    

---

### `alembic.ini` и `migrations/`

- Настройка и миграции базы данных через Alembic
    

---

### `.env`

- Хранение конфиденциальных настроек: DATABASE_URL, SECRET_KEY, DEBUG
    

---

## 3️⃣ Рекомендации по структуре

1. Разделяй **API, модели, схемы и бизнес-логику**
    
2. Используй **версии API** (`v1`, `v2`) для масштабируемости
    
3. Отделяй **зависимости и утилиты** для переиспользования
    
4. Для тестов создавай отдельный каталог и повторяй структуру приложения
    
5. Все конфигурации — через `.env` и Pydantic `BaseSettings`
    

---

💡 **Итог:**  
Структура должна быть **логичной, модульной и расширяемой**.  
Разделение на модели, схемы, сервисы и роуты делает проект удобным для поддержки и масштабирования.
