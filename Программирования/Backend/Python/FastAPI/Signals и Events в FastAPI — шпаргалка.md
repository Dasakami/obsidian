
FastAPI не имеет встроенных Django-style сигналов, но поддерживает **события приложения** (startup, shutdown) и **фоновые задачи (Background Tasks)**.  
Это позволяет выполнять операции при запуске приложения, завершении или асинхронно после запроса.

---

## 1️⃣ Startup событие

```python
from fastapi import FastAPI

app = FastAPI()

@app.on_event("startup")
async def startup_event():
    print("Приложение запускается...")
    # Можно подключить БД, Redis, очередь задач
````

- Вызывается **один раз при старте** сервера
    
- Полезно для инициализации сервисов, кэша, БД
    

---

## 2️⃣ Shutdown событие

```python
@app.on_event("shutdown")
async def shutdown_event():
    print("Приложение завершает работу...")
    # Можно закрыть соединения с БД, очистить ресурсы
```

- Вызывается при завершении работы сервера
    
- Закрывает соединения, очищает фоновые задачи
    

---

## 3️⃣ Background Tasks (асинхронные задачи)

```python
from fastapi import BackgroundTasks, FastAPI

app = FastAPI()

def write_log(message: str):
    with open("log.txt", "a") as f:
        f.write(f"{message}\n")

@app.post("/send-notification/")
async def send_notification(background_tasks: BackgroundTasks, msg: str):
    background_tasks.add_task(write_log, msg)
    return {"message": "Notification scheduled"}
```

- `BackgroundTasks` позволяет выполнять функции **асинхронно после ответа**
    
- Не блокирует основной поток приложения
    
- Подходит для логирования, отправки писем, уведомлений
    

---

## 4️⃣ Использование async функций в Background Tasks

```python
import asyncio
from fastapi import BackgroundTasks

async def async_task(data: str):
    await asyncio.sleep(3)
    print(f"Task completed: {data}")

@app.post("/async-task/")
async def run_async_task(background_tasks: BackgroundTasks, data: str):
    background_tasks.add_task(async_task, data)
    return {"status": "Task scheduled"}
```

- Можно добавлять **async функции**
    
- FastAPI корректно запускает их после ответа
    

---

## 5️⃣ Event-driven подход через кастомные функции

```python
# custom_events.py
event_listeners = []

def on_event(callback):
    event_listeners.append(callback)

def trigger_event(data):
    for callback in event_listeners:
        callback(data)

# main.py
from fastapi import FastAPI
from custom_events import on_event, trigger_event

app = FastAPI()

def listener(data):
    print(f"Event received: {data}")

on_event(listener)

@app.post("/trigger/")
def trigger(msg: str):
    trigger_event(msg)
    return {"status": "Event triggered"}
```

- Можно создавать свои “сигналы” через функции и список слушателей
    
- Имитирует Django signals для кастомных событий
    

---

## 6️⃣ Примеры использования

1. **Логирование** всех запросов в фоновой задаче
    
2. **Отправка уведомлений** пользователям после создания объекта
    
3. **Обновление кэша** при изменении данных
    
4. **Инициализация внешних сервисов** при старте приложения
    

---

## 🔹 Best practices

1. Используйте **startup/shutdown** для инициализации и закрытия ресурсов
    
2. Фоновые задачи через `BackgroundTasks` для неблокирующих операций
    
3. Для сложных проектов создавайте свои **event dispatcher** или используйте библиотеки
    
4. Не используйте BackgroundTasks для критически важных операций (лучше Celery/RQ)
    
5. Разделяйте sync и async задачи корректно
    

---

💡 **Итог:**  
FastAPI использует **events и background tasks** вместо сигналов Django.  
Это позволяет выполнять асинхронные операции, инициализацию и обработку событий без блокировки приложения.

```

---

Если хочешь, следующим шагом могу сделать **13_Тестирование.md**, чтобы подробно разобрать **pytest, TestClient, тестирование роутов и зависимостей** для FastAPI.  

Делаем его следующим?
```