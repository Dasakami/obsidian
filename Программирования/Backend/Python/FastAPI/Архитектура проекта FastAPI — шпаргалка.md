FastAPI позволяет строить **чистую, модульную и масштабируемую архитектуру**, похожую на Django по разделению на приложения.  

---

## 1️⃣ Общая структура проекта

```

fastapi_project/  
│  
├── app/  
│ ├── **init**.py  
│ ├── main.py # Точка входа, подключение роутеров  
│ ├── config.py # Настройки приложения (env, DB, секреты)  
│ ├── core/  
│ │ ├── **init**.py  
│ │ ├── security.py # JWT, хэширование паролей  
│ │ ├── exceptions.py # Кастомные исключения  
│ │ └── utils.py # Вспомогательные функции  
│ │  
│ ├── db/  
│ │ ├── **init**.py  
│ │ ├── base.py # Общие модели (Base)  
│ │ ├── session.py # SQLAlchemy сессии  
│ │ └── models/  
│ │ ├── **init**.py  
│ │ ├── user.py  
│ │ └── ...  
│ │  
│ ├── api/  
│ │ ├── **init**.py  
│ │ ├── deps.py # Dependencies (Depends)  
│ │ ├── v1/  
│ │ │ ├── **init**.py  
│ │ │ ├── routers/  
│ │ │ │ ├── **init**.py  
│ │ │ │ ├── auth.py  
│ │ │ │ ├── users.py  
│ │ │ │ └── items.py  
│ │ │ └── schemas.py  
│ │ └── v2/ # Для будущих версий API  
│ │  
│ ├── services/  
│ │ ├── **init**.py  
│ │ ├── auth_service.py  
│ │ └── user_service.py  
│ │  
│ ├── tests/  
│ │ ├── **init**.py  
│ │ ├── conftest.py # Фикстуры для pytest  
│ │ ├── test_users.py  
│ │ └── test_auth.py  
│ │  
│ └── static/ # Статика и медиа  
│  
├── requirements.txt  
├── alembic.ini # Миграции базы данных (Alembic)  
├── migrations/  
└── README.md

````

---

## 2️⃣ Объяснение слоёв

### app/main.py
- **Точка входа приложения**
- Подключает роутеры через `include_router`
- Запускается uvicorn

```python
from fastapi import FastAPI
from app.api.v1.routers import auth, users

app = FastAPI(title="FastAPI Project")

app.include_router(auth.router, prefix="/api/v1/auth", tags=["auth"])
app.include_router(users.router, prefix="/api/v1/users", tags=["users"])
````

---

### app/config.py

- Настройки проекта через **Pydantic**
    
- База данных, секретные ключи, алгоритмы JWT
    

```python
from pydantic import BaseSettings

class Settings(BaseSettings):
    DATABASE_URL: str = "sqlite:///./test.db"
    SECRET_KEY: str = "supersecret"
    ALGORITHM: str = "HS256"
    ACCESS_TOKEN_EXPIRE_MINUTES: int = 60

settings = Settings()
```

---

### app/db/

- **Модуль работы с базой данных**
    
- `base.py` — общая модель Base
    
- `session.py` — сессии SQLAlchemy
    
- `models/` — модели таблиц
    

---

### app/api/

- **Роуты и схемы**
    
- `deps.py` — общие зависимости через `Depends`
    
- `v1/` и `v2/` — версии API
    
- `routers/` — разбиение на эндпоинты
    
- `schemas.py` — Pydantic модели для валидации запросов и ответов
    

---

### app/services/

- **Бизнес-логика**
    
- Сервисные функции для роутов
    
- JWT, CRUD, работа с пользователями и объектами
    
- Очищает код роутеров от логики
    

---

### app/tests/

- **Тестирование проекта**
    
- `conftest.py` — фикстуры pytest для DB и клиента
    
- Тесты для моделей, роутеров, auth и CRUD
    

---

### app/static/

- Статика и медиа
    
- Используется при отдаче файлов через FastAPI
    

---

### Alembic / migrations

- Для миграций базы данных
    
- Alembic управляет версиями схем SQLAlchemy
    

---

## 3️⃣ Best practices

1. Разделяй **роуты, схемы, сервисы и модели**
    
2. Используй **версии API (v1, v2)** для совместимости
    
3. Настройки через Pydantic — удобно менять через `.env`
    
4. Тесты должны быть рядом с кодом приложения (`tests/`)
    
5. Middleware и кастомные обработчики ошибок отдельно в `core/`
    
6. Используй `services/` для бизнес-логики, чтобы роутеры оставались чистыми
    

---

💡 **Итог:**  
Эта структура позволяет быстро масштабировать проект, добавлять новые модули и версии API, легко подключать базу данных и тесты.
