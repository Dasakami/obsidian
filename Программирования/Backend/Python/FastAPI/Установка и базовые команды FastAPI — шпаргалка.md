FastAPI — современный Python-фреймворк для создания быстрых API с автоматической документацией.

---

## 1️⃣ Установка FastAPI

### 1. Создание виртуального окружения

Рекомендуется создавать виртуальное окружение, чтобы изолировать зависимости проекта:

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# macOS / Linux
python3 -m venv venv
source venv/bin/activate
````

---

### 2. Установка FastAPI и Uvicorn

```bash
pip install fastapi uvicorn
```

- `fastapi` — сам фреймворк
    
- `uvicorn` — ASGI-сервер для запуска приложения
    

---

### 3. Проверка установки

```bash
python -m pip show fastapi
python -m pip show uvicorn
```

---

## 2️⃣ Создание первого приложения

Создаём файл `main.py`:

```python
from fastapi import FastAPI

app = FastAPI()

@app.get("/")
def read_root():
    return {"message": "Hello, FastAPI!"}
```

Запуск:

```bash
uvicorn main:app --reload
```

- `main` — имя файла без `.py`
    
- `app` — объект FastAPI
    
- `--reload` — перезапуск сервера при изменениях
    

Откройте в браузере: [http://127.0.0.1:8000](http://127.0.0.1:8000/)

- Документация автоматически генерируется:
    
    - Swagger UI: `/docs`
        
    - ReDoc: `/redoc`
        

---

## 3️⃣ Базовые команды Uvicorn

```bash
# Запуск сервера
uvicorn main:app --reload

# Указание хоста и порта
uvicorn main:app --host 0.0.0.0 --port 5000 --reload

# Запуск в продакшене (без reload)
uvicorn main:app --host 0.0.0.0 --port 8000
```

---

## 4️⃣ Структура проекта на старте

```
project_name/
├── main.py          # Точка входа приложения
├── requirements.txt # Список зависимостей
└── venv/            # Виртуальное окружение
```

---

## 5️⃣ Установка дополнительных библиотек

- Для работы с базой данных:
    

```bash
pip install sqlalchemy psycopg2-binary alembic
```

- Для асинхронных баз данных:
    

```bash
pip install asyncpg
pip install databases
```

- Для тестирования:
    

```bash
pip install pytest httpx
```

- Для валидации данных:
    

```bash
pip install pydantic
```

- Для пагинации:
    

```bash
pip install fastapi-pagination
```

---

## 6️⃣ Работа с requirements.txt

```bash
# Сохранение зависимостей
pip freeze > requirements.txt

# Установка зависимостей из файла
pip install -r requirements.txt
```

---

## 7️⃣ Полезные команды для разработки

```bash
# Проверка версии Python
python --version

# Проверка установленных пакетов
pip list

# Обновление пакетов
pip install --upgrade fastapi uvicorn

# Удаление пакета
pip uninstall package_name
```

---

💡 **Итог:**

1. Создаем виртуальное окружение
    
2. Устанавливаем FastAPI и Uvicorn
    
3. Пишем первый эндпоинт
    
4. Используем автоматическую документацию
    
5. Настраиваем зависимости через `requirements.txt`
    
6. Используем базовые команды для запуска и тестирования приложения
    

