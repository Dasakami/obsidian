
# База

## 🔹 1. Проверка установки
```bash
docker --version
```
▶ Проверить установленный Docker и его версию.  

```bash
docker info
```
▶ Информация о Docker (система, контейнеры, образы).  

---

## 🔹 2. Работа с образами (images)

```bash
docker pull ubuntu
```
▶ Скачать образ `ubuntu` из Docker Hub.  

```bash
docker images
```
▶ Список локальных образов.  

```bash
docker rmi ubuntu
```
▶ Удалить образ `ubuntu`.  

---

## 🔹 3. Работа с контейнерами

```bash
docker run hello-world
```
▶ Запустить контейнер из образа `hello-world`.  

```bash
docker run -it ubuntu bash
```
▶ Запустить контейнер `ubuntu` с интерактивной консолью.  

```bash
docker ps
```
▶ Список запущенных контейнеров.  

```bash
docker ps -a
```
▶ Список всех контейнеров (включая остановленные).  

```bash
docker stop <id>
```
▶ Остановить контейнер.  

```bash
docker start <id>
```
▶ Запустить ранее остановленный контейнер.  

```bash
docker rm <id>
```
▶ Удалить контейнер.  

---

## 🔹 4. Работа с приложениями

```bash
docker build -t myapp .
```
▶ Собрать образ из `Dockerfile` в текущей папке с именем `myapp`.  

```bash
docker run -p 8080:80 myapp
```
▶ Запустить контейнер и пробросить порты (`локальный 8080 → контейнерный 80`).  

```bash
docker exec -it <id> bash
```
▶ Зайти внутрь работающего контейнера.  

---

## 🔹 5. Работа с Volumes (данные)

```bash
docker volume ls
```
▶ Список volumes.  

```bash
docker run -v mydata:/data ubuntu
```
▶ Подключить volume `mydata` к `/data` внутри контейнера.  

---

## 🔹 6. Очистка

```bash
docker system prune -a
```
▶ Удалить все неиспользуемые контейнеры, образы и кэш.  

---

# 📌 Мини-сценарий «от кода до запуска»

1. Создаём `Dockerfile`:
```dockerfile
FROM python:3.11
WORKDIR /app
COPY . .
RUN pip install -r requirements.txt
CMD ["python", "main.py"]
```

2. Собираем образ:
```bash
docker build -t myapp .
```

3. Запускаем контейнер:
```bash
docker run -p 8000:8000 myapp
```

---

Хочешь, я сделаю ещё такую же шпаргалку по **Docker Compose** (чтобы запускать несколько сервисов — например, Django + PostgreSQL)?